apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
spec:
  type: LoadBalancer   # or NodePort if you don’t want a cloud LB
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
